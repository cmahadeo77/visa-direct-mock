{
  "name": "visa-direct-mock",
  "version": "1.0.0",
  "description": "Node.js mock server for Visa Direct OCT with amount-based scenario triggers and a frontend UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
